using UnityEngine;

namespace Abracodabra.Genes
{
    /// <summary>
    /// Temporary marker component for fruit spawn points.
    /// These are created dynamically when fruits need to spawn and destroyed afterward.
    /// </summary>
    public class TemporaryFruitSpawnMarker : MonoBehaviour
    {
        // This component has no logic; it's just a tag.
    }
}